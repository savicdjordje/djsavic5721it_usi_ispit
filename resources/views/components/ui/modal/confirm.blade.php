<div {{ $attributes }}>
    <div class="bg-white shadow rounded p-6 space-y-4">
        <h2 class="text-lg font-semibold">{{ $title }}</h2>
        <div>{{ $content }}</div>
        <div class="flex justify-end gap-2">{{ $footer }}</div>
    </div>
</div>
